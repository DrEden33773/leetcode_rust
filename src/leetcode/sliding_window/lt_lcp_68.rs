#![allow(unused)]

pub struct Solution;

impl Solution {
  pub fn beautiful_bouquet(flowers: Vec<i32>, cnt: i32) -> i32 {
    use std::collections::HashMap;

    const MOD: i32 = 1e9 as i32 + 7;
    let mut res = 0;

    let sliding_window = |len: usize| {
      let mut l = 0;
      let mut r = 0;
      // 1. first window
    };

    unimplemented!();

    res % MOD
  }
}
